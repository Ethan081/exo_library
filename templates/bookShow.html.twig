{% extends "base.html.twig" %}
{% block title %}Show Livre{% endblock %}
    {% block content %}
        <main>
            <section class="section">
                <div class="card">
                    <h1>Titre : {{ book.title() }}</h1>
                    <h2>Genre : {{ book.genre() }}</h2>
                    <h3>Nombre de pages : {{ book.numberOfPages()}}</h3>
                    <p>Resumer :<br>{{ book.summary() }}</p>
                </div>
            </section>
        </main>
    {% endblock %}
