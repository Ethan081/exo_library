{% extends "base.html.twig" %}
{% block title %}Auteur Form{% endblock %}

{% block content %}
    {{ form_start(authorFormView, { 'attr': {'class': 'container'}}) }}

        {{ form_label(authorFormView.firstName, 'Titre', {'label_attr':{'class':'lableAuthor'}}) }}

        {{ form_widget(authorFormView.firstName, { 'attr': {'class': 'inputAuthor'}}) }}



        {{ form_label(authorFormView.lastName) }}
        {{ form_widget(authorFormView.lastName) }}

        {{ form_label(authorFormView.birthdate) }}
        {{ form_widget(authorFormView.birthdate) }}

        {{ form_label(authorFormView.deathdate) }}
        {{ form_widget(authorFormView.deathdate) }}

        {{ form_label(authorFormView.bio) }}
        {{ form_widget(authorFormView.bio) }}

        {{ form_label(authorFormView.enregister) }}
        {{ form_widget(authorFormView.enregister) }}

    {{ form_end(authorFormView) }}

    <div class="boite"></div>
{% endblock %}